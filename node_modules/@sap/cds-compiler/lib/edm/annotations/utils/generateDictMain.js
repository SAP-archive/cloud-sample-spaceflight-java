//
// wrapper program
// read odata vocabulary xml files and produce Dictionary.json
//
'use strict';

var genDict = require('./generateDictionary')

genDict.buildDictionary();

// eslint-disable-next-line no-console
console.log(JSON.stringify(genDict.dict, null, 2));
