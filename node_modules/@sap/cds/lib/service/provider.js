const cds = require('../index.js')
const services = require('@sap/cds-services'); services.inject(cds) 
const factory = services.service
'impl' in factory || Object.defineProperties (factory, {
  impl: {value: fn=>fn }, // dummy function to assist vscode typings-based IntelliSense
  kind: {value: 'service' },  // used for cds.reflect
  prototype: {value: cds.builtin.types.service },  // wire it to the core class for cds.linked
})
module.exports = factory
