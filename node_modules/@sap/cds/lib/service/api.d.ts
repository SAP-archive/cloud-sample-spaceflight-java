const cds = {
    serve (model) : { 
        with (impl : ServiceImpl)
        at (path : string)
        to (channel : string)
        in (app : express.Router)
    } 
}

interface ServiceProvider {
    path : string
    impl : ServiceImpl
    on (event, entity, handler) : this
    before (event, entity, handler) : this
    after (event, entity, handler) : this
}

interface ProtocolAdapter {
    provider : ServiceProvider
}

interface ServiceImpl { ( this: ServiceProvider, entities ) }