'use strict';

/**
 * @abstract
 */
class AbstractEncoder {

    /**
     * @param {Buffer|boolean|string|number} value
     * @param {EdmPrimitiveType} type
     * @return {string}
     */
    // eslint-disable-next-line no-unused-vars
    encodeUriLiteral(value, type) {
    }

    /**
     * @param {string} name
     * @param {string} value
     * @returns {string}
     */
    // eslint-disable-next-line no-unused-vars
    encodeQueryOption(name, value) {
    }
}

module.exports = AbstractEncoder;
