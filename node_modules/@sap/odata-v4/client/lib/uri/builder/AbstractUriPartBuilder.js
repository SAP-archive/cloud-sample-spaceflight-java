'use strict';

/**
 * @abstract
 */
class AbstractUriPartBuilder {

    /**
     * builds the uri segment based on the collected Information from the ODataUriBuilder
     * @param {SegmentInfo} segmentInfo
     * @return {string}
     */
    // eslint-disable-next-line no-unused-vars
    buildSegment(segmentInfo) {
    }

    /**
     * @param {QueryOptions} option
     * @param {*} value
     * @return {{name: QueryOptions|string, value: string}}
     */
    // eslint-disable-next-line no-unused-vars
    buildQueryOption(option, value) {
    }

}

module.exports = AbstractUriPartBuilder;
