{
  "name": "spaceflight",
  "version": "0.1.0",
  "dependencies": {
    "@sap/cds": "git://github.wdf.sap.corp/cdx/cds"
  },
  "devDependencies": {
    "npm-watch": "^0.3.0"
  },
  "watch": {
    "build": {
      "patterns": ["db", "srv"],
      "extensions": "cds",
      "inherit": true
    }
  },
  "scripts": {
    "build": "cds build --clean",
    "watch": "npm-watch"
  },
  "license": "ISC"
}
