{
  "name": "spaceflight",
  "version": "0.1.0",
  "dependencies": {
    "@sap/cds": "2.x"
  },
  "devDependencies": {
    "npm-watch": "^0.3.0"
  },
  "watch": {
    "build": {
      "patterns": ["db", "srv"],
      "extensions": "cds",
      "inherit": true
    }
  },
  "scripts": {
    "build": "cds build --clean",
    "watch": "npm-watch"
  },
  "license": "ISC"
}
